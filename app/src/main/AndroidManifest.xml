<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.brunofrsantos.pedbolaapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icone_round"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icone_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.ListaJogadoresSelecionaveisActivity" />
        <activity android:name=".ui.DadosJogadorActivity" />
        <activity android:name=".ui.VerJogadorActivity" />
        <activity android:name=".ui.NovoJogadorActivity" />
        <activity android:name=".ui.NovoJogoActivity" />
        <activity android:name=".ui.MenuActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>